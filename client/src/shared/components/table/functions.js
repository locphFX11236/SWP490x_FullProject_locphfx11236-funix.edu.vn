export const PlusData = (key) => {
    if (key === 'programs') return console.log(key)
    else if (key === 'users') return console.log(key)
    else return console.log('Lỗi key! In <TitleTable/>');
};