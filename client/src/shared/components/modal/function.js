export const DeleteProgram = (record) => {
    console.log('Delete! with record', record);
};

export const UpdateProgram = (record) => {
    console.log('Update! with record', record);
};

export const CreateProgram = (record) => {
    console.log('Create! with record', record);
};

export const CancelProgram = (record) => {
    console.log('Cancel! with record', record);
};